# Docker и Docker Compose

**Docker** позволяет упаковать приложение вместе с его окружением в контейнер. Благодаря этому Go-сервис запускается одинаково на компьютере разработчика, в CI и на сервере.

## Основные понятия

| Термин | Смысл |
|---|---|
| **Image (образ)** | Неизменяемый шаблон: приложение, библиотеки и настройки для запуска |
| **Container (контейнер)** | Запущенный экземпляр образа |
| **Dockerfile** | Рецепт сборки образа |
| **Volume (том)** | Данные вне жизненного цикла контейнера |
| **Docker Compose** | Описание и запуск нескольких связанных контейнеров одним YAML-файлом |

Контейнер не является виртуальной машиной: он использует ядро хостовой ОС, но изолирует процессы, сеть и файловую систему приложения.

Для Linux-контейнеров на macOS/Windows Docker Desktop использует Linux VM. Контейнеры делят её ядро, а не напрямую ядро macOS/Windows. Одинаковый образ не отменяет различий архитектуры CPU, конфигурации и внешних зависимостей.

## Dockerfile для Go-сервиса

Учебный пример предполагает точку входа `./cmd/api` и наличие `go.sum`; адаптируй их к своему проекту. Версию Go передаём при сборке в соответствии с `go.mod` и политикой обновления.

```dockerfile
# Сборка приложения
ARG GO_VERSION
FROM golang:${GO_VERSION} AS build
WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 go build -o server ./cmd/api

# Финальный образ: только бинарный файл
FROM alpine:3.22
RUN apk add --no-cache ca-certificates && adduser -D -u 10001 appuser
WORKDIR /app
COPY --from=build /app/server ./server
USER appuser

EXPOSE 8080
CMD ["./server"]
```

Это **multi-stage build**: на первом этапе есть компилятор Go, на втором — только готовый бинарник. Финальный образ получается меньше и содержит меньше ненужных инструментов.

Полезный `.dockerignore` не даёт отправлять в контекст сборки лишние файлы:

```text
.git
.env
tmp/
bin/
```

Собрать и запустить образ можно так:

```bash
docker build --build-arg GO_VERSION=1.27 -t playlist-api .
docker run --rm -p 8080:8080 playlist-api
```

`-p 8080:8080` публикует порт контейнера наружу в формате `порт_хоста:порт_контейнера`.

`EXPOSE` сам порт не публикует. HTTP-сервер должен слушать `:8080`, а не только `127.0.0.1:8080` внутри контейнера. Для production фиксируют проверенную patch-версию и/или digest образа и регулярно обновляют его. `CGO_ENABLED=0` подходит не для всех зависимостей.

## Docker Compose: Go-сервис и PostgreSQL

Когда для приложения нужны сервис и БД, вручную запускать контейнеры неудобно. Compose описывает их как один проект.

```yaml
services:
  api:
    build:
      context: .
      args:
        GO_VERSION: "1.27"
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: postgres://app:app@postgres:5432/app?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
```

```mermaid
flowchart LR
    C[Client] -->|localhost:8080| A[api container]
    A -->|postgres:5432| P[postgres container]
    P --> V[(postgres_data volume)]
```

Внутри сети Compose сервисы обращаются друг к другу по имени сервиса. Поэтому Go-приложение подключается к `postgres:5432`, а не к `localhost:5432`: `localhost` внутри контейнера означает сам контейнер `api`.

Команды:

```bash
docker compose up --build
docker compose up -d
docker compose logs -f api
docker compose down
```

`down` удалит контейнеры и сеть, но именованный том останется. Команда `docker compose down -v` удалит также тома — для данных PostgreSQL это разрушительное действие.

## Volumes, переменные и сеть

- Без тома данные внутри контейнера исчезнут после его удаления. PostgreSQL нужен именованный volume.
- Переменные окружения подходят для конфигурации. Пароли не стоит вшивать в образ или коммитить в Git; для локальной разработки используют неотслеживаемый `.env`, в production — менеджер секретов.
- Внешние порты публикуют только тем сервисам, к которым должен обращаться хост. PostgreSQL для локальной разработки можно не публиковать, если доступ к нему нужен лишь `api`.

Перезапуск контейнера не равен его удалению: записываемый слой переживает restart, но не замену контейнера. **Bind mount** подключает конкретный путь хоста, а именованным volume управляет Docker. `.env` в Compose используется для подстановок: значения не становятся окружением контейнера без `environment`/`env_file`. Пароль `app` и `sslmode=disable` выше допустимы только для учебного локального окружения.

## Сборка, завершение и CI

`RUN` выполняется при сборке; `CMD` задаёт стандартную команду запуска; `ENTRYPOINT` — исполняемый файл, к которому могут добавляться аргументы. JSON-форма команды упрощает доставку сигналов процессу. Go-сервис должен обрабатывать завершение и успевать закончить активные запросы до принудительной остановки.

Кеш слоёв ускоряет сборку: сначала копируют `go.mod`/`go.sum` и загружают зависимости, затем исходники. Изменение раннего слоя инвалидирует последующие.

Минимальная цепочка **CI/CD** — автоматической проверки и доставки: тесты → `go vet`/`-race` → проверка уязвимостей → сборка образа → registry → совместимые миграции и deploy → проверка состояния. Образ маркируют версией/commit; rollback приложения возможен только при совместимой схеме БД.

`depends_on` с healthcheck помогает при начальном запуске, но не лечит последующее падение БД. Приложению всё равно нужны таймауты, восстановление соединений и обработка ошибок.

## Типичные ошибки

- Считать, что `depends_on` без healthcheck означает готовность БД. Контейнер мог стартовать, но PostgreSQL ещё не принимать подключения.
- Использовать `latest`: лучше фиксировать версию образа, например `postgres:17`.
- Хранить данные БД только внутри контейнера.
- Копировать в образ `.env`, ключи или весь репозиторий без `.dockerignore`.
- Путать порт хоста с портом контейнера и использовать `localhost` для связи двух контейнеров.

## Что важно на собеседовании

1. Образ — шаблон, контейнер — его запущенный экземпляр.
2. Dockerfile собирает один образ; Docker Compose запускает несколько связанных сервисов.
3. Volume сохраняет данные после пересоздания контейнера.
4. Сервисы Compose общаются по DNS-имени сервиса во внутренней сети.
5. Multi-stage build уменьшает финальный образ и отделяет сборку от запуска.

## Источники

- [Docker: что такое контейнер](https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/)
- [Docker: Dockerfile](https://docs.docker.com/get-started/docker-concepts/building-images/writing-a-dockerfile/)
- [Docker Compose Quickstart](https://docs.docker.com/compose/gettingstarted/)
- [Docker: volumes](https://docs.docker.com/engine/storage/volumes/)
- [Docker: сети](https://docs.docker.com/engine/network/)
- [Docker: multi-stage builds](https://docs.docker.com/build/building/multi-stage/)

## Видео для закрепления

1. [Docker для начинающих за 10 минут: Docker Compose с нуля](https://www.youtube.com/watch?v=rr7vjLj9F_0) — рус., базовые команды и Compose.
2. [Docker in 100 Seconds — Fireship](https://www.youtube.com/watch?v=gAkwW2tuIqE) — англ., образ, контейнер и Dockerfile.
